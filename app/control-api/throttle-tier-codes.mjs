// These codes are the named values declared by the gateway deployment. Keeping the
// authoring choices beside the resolver prevents a valid-looking draft from naming a
// tier APIM cannot apply.
export const DEPLOYED_THROTTLE_TIER_CODES = Object.freeze(['tier-reduced', 'tier-minimal']);
